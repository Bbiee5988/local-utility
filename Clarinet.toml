[project]
name = "local-utility"
authors = []
description = "A decentralized platform for community resource management"
telemetry = false
requirements = []
[contracts.local-vault]
path = "contracts/local-vault.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = true
trusted_sender = false
trusted_caller = false
callee_filter = false
